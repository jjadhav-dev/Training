const {user} = require('../models');

const registerUserService = async(userData)=>{
    try {
        
    } catch (error) {
        throw error;
    }
}

module.exports = {
    registerUserService
}