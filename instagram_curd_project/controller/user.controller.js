const { registerUserService } = require('../services/user.service');
const registerUser = async (req, res) => {
    try {

    } catch (error) {
        next(error);
    }
}

module.exports = {
    registerUser
}